<!DOCTYPE html>
<html lang="en">
<head>
  <% include ../partials/head %>
</head>

<body class="container-fluid">
  <header>
    <% include ../partials/header %>
  </header>

  <main>
    <div>
      <h3>Edit Free Food Event:</h3>
      <form class="food-form" method="POST" action="/foods/<%= food._id %>?_method=PUT">
        <% include ../partials/food-form %>
        <a href="/foods/<%= food._id %>" class="btn btn-primary">Back</a>
        <button type="submit" class="btn btn-success">Save</button>
      </form>
    </div>
  </main>

  <footer>
    <% include ../partials/footer %>
  </footer>
</body>
</html>
